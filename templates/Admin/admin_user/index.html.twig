{% extends 'base.admin.html.twig' %}

{% block title %}Gérer les utilisateurs{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Liste des utilisateurs</h1>
        <div class="row justify-content-center table-responsive">
            <table class="table table-dark table-striped align-middle">
                <tr class="text-center">
                    <th class="col-3">Email</th>
                    <th class="col-3">Pseudo</th>
                    <th class="col-3">Date d'inscription</th>
                    <th class="col-3">Action</th>
                </tr>
                {% for user in users %}
                    <tr class="text-center">
                        <td>{{ user.email }}</td>
                        <td>{{ user.username }}</td>
                        <td>{{ user.createdAt|date("d/m/Y") }}</td>
                        <td>
                            <div class="d-flex flex-row justify-content-evenly">
                                <a class="text-light" href="{{ path('app_admin_user_delete', {'id': user.id}) }}"><i class="fa-solid fa-trash"></i></a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
        {# display navigation #}
        <div class="navigation d-flex justify-content-center my-2">
            {{ knp_pagination_render(users) }}
        </div>
    </div>
{% endblock %}
